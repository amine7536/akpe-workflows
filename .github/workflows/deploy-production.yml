name: Deploy Production

on:
  workflow_call:
    inputs:
      service_name:
        description: "Service name (e.g. backend-1, backend-2, front)"
        required: true
        type: string
      gitops_repo:
        description: "Gitops repo in owner/repo format (default: vars.GITOPS_REPO)"
        required: false
        type: string
        default: ""

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    concurrency:
      group: gitops-update
      cancel-in-progress: false
    steps:
      - name: Checkout gitops repo
        uses: actions/checkout@v6
        with:
          repository: ${{ inputs.gitops_repo || vars.GITOPS_REPO }}
          token: ${{ secrets.GITOPS_PAT }}

      - name: Update image tag
        run: |
          yq -i '(.spec.source.helm.parameters[] | select(.name == "image.tag")).value = "${{ github.sha }}"' production/${{ inputs.service_name }}.yaml

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --staged --quiet && exit 0
          git commit -m "chore(${{ inputs.service_name }}): update image tag to ${{ github.sha }}"
          git push
